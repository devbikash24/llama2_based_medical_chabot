from setuptools import find_packages, setup

setup(
    name="Medical Chatbot",
    version='0.0.0',
    author='Bikash Dev',
    author_email='devbikash24@gmail.com',
    description='A simple medical chatbot using LangChain',
    packages=find_packages()
)  # Ensure there is a newline after this line